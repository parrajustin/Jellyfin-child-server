# Build context of the e2e runner image (tests/e2e). Host artifacts must not leak into the
# image: node_modules would overwrite the Linux install done by `npm ci`.
node_modules
test-results
playwright-report
.work
*.log
